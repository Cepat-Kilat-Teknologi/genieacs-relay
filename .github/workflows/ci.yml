name: Go CI Pipeline

on:
  push:
    branches: [ "main", "develop" ]
    tags: [ "v*.*.*" ]
  pull_request:
    branches: [ "main", "develop" ]
  workflow_dispatch:

env:
  REGISTRY: docker.io
  IMAGE_NAME: cepatkilatteknologi/acs-api-gateway
  GO_VERSION: '1.25'

jobs:
  # ========================
  # Job 1: Lint
  # ========================
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go.sum
      
      - name: Install Linter
        run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
      
      - name: Run Linter
        run: golangci-lint run --timeout=5m --verbose
  
  # ========================
  # Job 2: Unit Tests
  # ========================
  tests:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      
      - name: Run Unit Tests with Coverage
        run: go test -v -race -coverprofile=coverage.out ./...
      
      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.out
  
  # ========================
  # Job 3: Govulncheck
  # ========================
  govulncheck:
    runs-on: ubuntu-latest
    needs: tests
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      
      - name: Run Govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...
  
  # ========================
  # Job 4: Build Image
  # ========================
  build:
    runs-on: ubuntu-latest
    needs: govulncheck
    environment: ci
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=develop,enable=${{ github.ref == 'refs/heads/develop' }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
      
      - name: Build Docker Image (no push yet)
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          target: production
          platforms: linux/amd64,linux/arm64
          push: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Save Docker image as artifact
        run: docker save ${{ steps.meta.outputs.tags }} -o image.tar
      - uses: actions/upload-artifact@v4
        with:
          name: built-image
          path: image.tar
  
  # ========================
  # Job 5: Scan Image with Trivy for Public Repos
  # ========================
#  scan:
#    runs-on: ubuntu-latest
#    needs: build
#    steps:
#      - uses: actions/download-artifact@v4
#        with:
#          name: built-image
#          path: .
#
#      - name: Load Docker image
#        run: docker load -i image.tar
#
#      - name: Scan Image with Trivy
#        uses: aquasecurity/trivy-action@0.24.0
#        with:
#          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
#          format: 'sarif'
#          output: 'trivy-results.sarif'
#          ignore-unfixed: true
#          exit-code: 1  # fail kalau ada HIGH/CRITICAL
#
#      - name: Upload Trivy Results as Artifact
#        uses: actions/upload-artifact@v4
#        with:
#          name: trivy-results
#          path: trivy-results.sarif
#
  # ========================
  # Job 6: Push Image to Registry If Scan Passed
  # ========================
  push:
    runs-on: ubuntu-latest
#    needs: scan # Uncomment this line to enforce scanning before pushing
    needs: build # Use this line to skip scanning and push directly after build
    environment: ci
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: built-image
          path: .
      
      - name: Load Docker image
        run: docker load -i image.tar
      
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Push Docker Image
        run: docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
